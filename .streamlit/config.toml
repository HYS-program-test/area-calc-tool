[runner]
# 關掉「允許新的重新整理直接打斷還在跑的舊的那次」，
# 讓自訂元件（圖面編輯區、空調負荷計算結果表格）有更充分的時間完成跟 Streamlit
# 的握手掛載，降低「畫面空白／元件顯示不出來」這類競速問題的發生機率。
# 這不是官方保證有效的正式解法（Streamlit 目前沒有針對這個問題的專門設定），
# 只是社群討論中被回報「有機會降低發生機率」的緩解措施。
fastReruns = false
